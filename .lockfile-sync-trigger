2026-08-14 06:24 UTC — unstick-sweep: package-lock.json out of sync with package.json on main (jest/ts-jest/@types/jest missing from lock). Every GHA job dies at npm ci; tsc gate red on ALL commits. This push triggers lockfile-sync.yml on this security branch to regenerate the lockfile (Node 20, npm audit, full build, auto-commit back). Next: verify branch head, fast-forward main via fn_gh_update_ref. Brief: ci-lockfile-resync-main-v1.