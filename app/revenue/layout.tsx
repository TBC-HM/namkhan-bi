import type { ReactNode } from 'react';

export const metadata = { title: 'Revenue — Namkhan BI' };

export default function RevenueLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
