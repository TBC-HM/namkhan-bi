import ComingSoon from '../_components/ComingSoon';
export const dynamic = 'force-dynamic';
export default function Page() {
  return (
    <ComingSoon
      tab="Groups"
      proposalLine="Group bookings pipeline — MICE, weddings, retreats, family blocks. Strategist + margin floor + displacement check."
    />
  );
}
