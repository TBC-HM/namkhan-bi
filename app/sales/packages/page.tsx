import ComingSoon from '../_components/ComingSoon';
export const dynamic = 'force-dynamic';
export default function Page() {
  return (
    <ComingSoon
      tab="Packages"
      proposalLine="Package builder — room × LOS × F&B × spa × activities × transfer. Margin floor + brand voice + vendor lock."
    />
  );
}
