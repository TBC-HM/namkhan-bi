import type { ReactNode } from 'react';

export const metadata = { title: 'Sales — Namkhan BI' };

export default function SalesLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
