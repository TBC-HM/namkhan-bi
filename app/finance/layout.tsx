import type { ReactNode } from 'react';

export const metadata = { title: 'Finance — Namkhan BI' };

export default function FinanceLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
