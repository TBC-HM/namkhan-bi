import type { ReactNode } from 'react';

export const metadata = { title: 'Marketing — Namkhan BI' };

export default function MarketingLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
