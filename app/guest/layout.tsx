import type { ReactNode } from 'react';

export const metadata = { title: 'Guest — Namkhan BI' };

export default function GuestLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
