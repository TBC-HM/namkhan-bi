// Redirect stub — Schemas moved to IT2 (it-area-reorg-v1 consolidation).
import { redirect } from 'next/navigation';
export default function LegacySchemasRedirect() {
  redirect('/holding/it2/knowledge/data/schemas');
}
