import type { ReactNode } from 'react';

export const metadata = { title: 'Operations — Namkhan BI' };

export default function OperationsLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
